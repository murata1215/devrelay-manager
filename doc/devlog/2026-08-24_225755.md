# 2026-08-24 22:57 JST ｜ サイクル1.5: orchestrator(層③) 設計doc追加

要求: 層③ orchestrator の設計スペックを、人間レビュー済みの確定内容として devrelay-manager の doc/ に verbatim で着地させる。コードには一切触れない。

実行: doc/orchestrator-layer3-design.md を新規作成。指定された DOC BODY（§0〜§13、全14セクション相当）をそのまま書き込んだ。§2 の Dispatch ステートマシンの ASCII 図（罫線・矢印・インデント）、【確定 D1】〜【確定 D4】・【確定 §10-1】・【確定 §10-2】・【no-silent-failure】の見出し内マーカー、§12 のチェックボックス（[x]4件・[ ]3件）、水平線14箇所をすべて原文のまま維持した。

検証: 書き込み後に全152行を読み返し、原文との突き合わせを実施。全角括弧・全角スラッシュ「／」・矢印記号「←▼└─」・引用符「」の使用箇所を含め、差異は見つからなかった（サイクル1.2で発生した半角/全角括弧の取り違えは今回は再現しなかった）。git status/diffで意図した3ファイル（新doc・新devlog・INDEX変更）のみが対象であることも確認予定。既存のapps/ scripts/ tests/ rules/ には一切触れていない。

発見: 本spec は層①(DB)・層②(core MCPアダプタ)の完了を前提として書かれており、Dispatch.status に draft/submitting/stale/approving の4値を追加する設計（String型のためスキーマ変更不要）が明記されている。また「orchestratorモデル選択」と「council実結線時のexecutor AI選択」が別軸である点、承認ゲートはタイムアウトで stopped にはせず、承認時点でのstaleCheck/freshCheckにより鮮度を担保する設計思想（no-silent-failureの徹底）が council-orchestration-spec.md の哲学と一貫している点が明記されていた。
