# 2026-09-03 19:51 JST ｜ サイクル1.23: UI刷新① Tailwind導入とレイアウト再構成

## 要求

1.22 の棚卸しで判明した apps/web の暫定実装（CSS変数ゼロ・ダークモード無し・レスポンシブ非対応の364行素CSS）を claude.ai 水準へ引き上げる刷新の前半として、Tailwind 4 導入・設計トークン定義・レイアウト再構成のみを行う。DispatchCard.tsx（249行・最大）と Markdown 描画は 1.24 へ送る2分割方針。council トグルは1.21で結線済みのため、要求文の「disabledのまま」は視覚的に控えめに配置する意図と解釈し、useState/DB列/core submit引数の結線は維持する（承認ノートで明示確認済み）。

## 実行

`apps/web` に `tailwindcss@4.3.3` / `@tailwindcss/vite@4.3.3` を devDependency 追加し、`vite.config.ts` へプラグイン登録（PostCSS設定ファイルは作らず）。`styles.css` を全面書換し、ニュートラル5役+アクセント1色+ステータス3色を `:root` の CSS変数として定義、`prefers-color-scheme` で自動反転させ、`@theme inline` で Tailwind トークンへマッピングした。DispatchCard.tsx を触らない制約のため、同ファイルが参照する22クラス（`dispatch-card__*` 16種・`badge--*` 6種）を `@layer components` へそのまま移植し色のみトークン参照化、run/council 用に `--info`/`--council-badge` の例外トークンを追加してコメントで明記した。App.tsx はポーリング `useEffect`（96-115行）と `pollTimerRef` を一切変更せず、`sidebarOpen` state 1個のみ追加してレイアウトを260pxサイドバー＋中央寄せタイムライン（max-w-3xl）の1カラム構成へ再構成、768px未満はオーバーレイ式に開閉する。Timeline.tsx は manager発言のバブルを廃止しプレーンテキスト化（本文描画方式は `pre-wrap` のまま変更せず）。ProjectPicker.tsx はチップ列化し Composer.tsx の直上に配置（JSX兄弟順序を入れ替え、DOM親子関係の変更は許可されていたため共通ラッパーで視覚的に一体化）。ThreadList.tsx・Composer.tsx も className をユーティリティへ差し替えた。

## 検証

`pnpm typecheck` はエラーなしで通過。`pnpm test` は29件 pass / 0 fail のまま（純粋関数を変更していないためテスト追加なし、想定通り）。`pnpm build` は成功し `dist/assets/index-kc40mYr-.css` 13.17KB（gzip 3.78KB）を生成、JS は210.93KB（gzip 66.78KB）。`git diff --stat` で変更が web側8ファイル＋依存追加に伴う `pnpm-lock.yaml` に限定され、`DispatchCard.tsx` / `src/lib/**` / `apps/server` に差分が無いことを確認した。実サーバー・devサーバーは起動していない。

## 発見

DispatchCard.tsx が参照する `.dispatch-card__summary` クラスは1.22時点の旧 `styles.css` にルールが存在しない孤児クラスだったため、今回の移植でも空ルールは追加しなかった（1.24で拾う）。Tailwind 4 の `@theme` 単体では `prefers-color-scheme` による動的な値の切り替えができず、`:root` にセマンティック変数を置き `@theme inline` で参照する二段構成が必要だった。council トグルの要求文「disabledのまま」は1.21の結線を巻き戻す指示に読めたため実装前に承認ノートで確認し、結線維持・配置のみ変更で確定した。
