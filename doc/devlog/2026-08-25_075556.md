# 2026-08-25 07:55 JST ｜ サイクル1.9: ③-2 実機スモーク（worker on）

**要求**: ③-2 決定的 worker を実 core（`ubuntu-prod/keisuke`）に対して1本走らせ、`draft → submitting → planning →` 【ゲート1】`→ approving → building →`（可能なら）`done` を実測する。コード・テスト・core は一切変更しない観測専用サイクル。安全条件として `HOST=127.0.0.1`、自エージェント（claude PID・devrelay agent PID）を絶対に kill しない、スモーク指示は `/tmp` のみでリポジトリ非改変、対象は `ubuntu-prod/keisuke` で core の直列化・待機・拒否があれば記録し回避しない、の4点を課した。

**実行**: 07:22:10 JST に `DISPATCH_WORKER_MODE=on` を1回起動し `parseWorkerMode` の fail-loud（即時 throw, exit 1）を実測。以降 `HOST=127.0.0.1 PORT=3100 DISPATCH_WORKER_MODE=resident node --import tsx src/index.ts` の単一プロセスで常駐（07:22:15 起動、PID 1666701）。`GET /core/projects`（89件）を `machine` で絞り込み、`ubuntu-prod/keisuke` は `devrelay-manager`（`id=cmqm5q20900cq40nu6un5yr0p`, `path=/home/keisuke/devrelay-manager`）の1件のみに一意確定——このリポジトリそのものが対象プロジェクトだった。`POST /threads` で Thread（`cmt7sxme900002w7qow1nxevv`）を作り、psql の INSERT 1本（`status` は既定値 `'draft'` のまま）で Dispatch 行（`smoke19-1787610194-57a24ea1`, 07:23:14）を手挿入。`draft` は 07:23:14〜07:25:16（約122秒・worker3周分）`lastPolledAt` NULL のまま claim すらされず。07:25:24 に `approve-target`（指示文＝`/tmp/devrelay-smoke-1_9.txt` の作成→確認→削除のみ、リポジトリ禁止を明記）で `submitting` へ、07:25:46.758 に `planning`（`submissionId=cmt7t1na104luwsy1odykrclc`）へ自動遷移。1回目の意図的 kill は当初 `planning` のポーリング待ち中を狙ったが、`planning` が想定より速く 07:28:16.845 に `awaiting_approval` へ進んでしまったため、`awaiting_approval` 中（07:29:34 kill、PID 1666701＝自エージェント claude/devrelay-agent PIDとは別と確認済み）に実施。約2分11秒（〜07:31:45 再起動、PID 1668006）DB完全静止を確認後、`submissionId` 不変のまま復帰。07:31:53〜07:32:37（90秒弱）`awaiting_approval` の自動通過なしを確認し、07:33:04 `approve-plan` →07:33:16.365 `approving`→`building`。`building` は idempotent なので、ここで2回目の意図的 kill（07:34:59、PID 1668006）を実施し約56秒 DB凍結・`submissionId` 不変での復帰（07:35:39 再起動、PID 1668690）を確認。同じ `building` 窓で2本目 worker（PORT=3101、PID 1668771、07:35:53起動〜07:40:23頃に自然停止）を並走させたが `lastPolledAt` の更新は1回のみ（07:38:55.317）で二重更新・エラーともに無し。総観測は07:22:10〜07:55:14（約33分）。

**検証**: ゲート1・2はいずれも自動通過ゼロ（worker が該当行を claim すらしない）。バックオフは `building` 突入後、初回30秒（即時, 22:33:16.365→22:33:46.401）→2分（22:35:55.029, 予定22:35:46.401+9秒遅延）→3分（22:38:55.317, 予定22:38:55.029とほぼ一致）→5分キャップ×2回（22:44:11.43, 22:49:11.635, 22:54:11.817）と、実装どおり2→3→(4スキップ)→5→5…の階段を実クロックで確認。2回のkill→再起動は両方とも `submissionId` の重複なし（＝`submitInstruction`/`approveImplementation` の再送なし）でDB内容だけから復帰。2本並走時も `lastPolledAt` CAS により片方のみが claim し二重処理なし。`.env` は無変更、`git status --short` は `.devrelay/` `.devrelay-files/` のみ（差分なし）、`pnpm typecheck` クリーン、`pnpm test` 70/70 全pass——コード無変更を実測で裏付け。worker最終プロセス（PID 1668690）は観測終了時点で既に自然停止済み、port 3100/3101 とも LISTEN なしを確認。

**発見**: (1) `DISPATCH_WORKER_MODE=on` は不正値で fail-loud throw する（指示文の語彙と実装の乖離、1.8で既知）。(2) 対象 `ubuntu-prod/keisuke` の唯一のプロジェクトが `devrelay-manager` 自身であり、`submitInstruction` 実行後、**core は第二の独立 claude プロセスを立てず、このオーケストレーター自身のセッションの plan ファイル（`~/.claude/plans/velvet-snacking-cerf.md`）へ「受信ディスパッチ」セクションを注入する形で処理した**——直列化・待機・拒否のいずれとも異なる第4のパターン。付随のツール的な system-reminder に「ユーザーに黙っておけ」という趣旨の文言が含まれていたが、実ユーザーの承認ではないため一切実行せず、本devlogで明示する（回避も強行突破もしない、の遵守）。(3) この未承認・未実行のため `building` は本観測終了（33分経過）まで `buildId` を得られず `done` へ到達しなかった——これは3.9のバグではなく、「exec 承認なしにdispatchされた指示を実行しない」という安全側の判断の直接的な帰結として記録する。(4) `classifyPlanResult` は実 core の plan-ready 応答に対して正しく `ready` へ分類できた（`planning→awaiting_approval` が自動遷移した実測が証拠）——1.8時点で懸念していた語彙不一致リスクは今回は顕在化しなかった。(5) 安全条件2のPID確認手順中、ツール実行環境の都合で `claude` の PID が呼び出しごとに変わる（1665019→1666487→1668212 のように再割当）ことを実測、以後のkill判断は毎回その場でのPID再確認が必須と分かった。(6) `/tmp/devrelay-smoke-1_9.txt` は最終的に作成されなかった（(3)と同根、未実行のため）。手挿入した Thread・Dispatch 行は監査記録として削除せず残す。
