# 2026-08-26 06:15 JST ｜ サイクル1.10: 再スモーク（投げ先変更）＋devlog重複整理

## 要求
サイクル1.9の実機スモークで唯一未検証のまま残った `classifyBuildResult` を、実coreの応答語彙で検証する。1.9は投げ先 `ubuntu-prod/keisuke` がオーケストレーター自身のリポジトリに解決され自己ループしたため `building` で足踏みし `buildId` が空のまま終わった。本サイクルは投げ先を自己以外（`ubuntu-prod/keisuke` でも `/opt/devrelay` でもない）に変えて `draft → submitting → planning →`【ゲート1】`→ awaiting_approval →`【ゲート2】`→ approving → building → done` を通す。あわせて1.9の重複devlog（`2026-08-25_075519.md`/`2026-08-25_075556.md`）の出自を特定し、履歴を書き換えずINDEXの記述だけで整理する。コード変更ゼロの観測専用サイクル。

## 実行
`GET /core/projects`（89件）を `ubuntu-prod/devrelay` かつ `/home/devrelay/testflight/` 配下で絞り込み、`test010`（`id=cmmlds6f900h2145t3tytgg6s`, `path=/home/devrelay/testflight/test010`, `online=true`）を選定（自己リポジトリでも `/opt/devrelay` でもなく使い捨てscaffoldのため実害なし）。`HOST=127.0.0.1 PORT=3100 DISPATCH_WORKER_MODE=resident`（`run_in_background`）で単一worker起動（PID 1739206、自プロセス claude 1739045/親1739161・以降1740110/1740566とは別と都度確認）。`POST /threads` でThread（`cmt95orkh00008bq2m73w6zzz`）を作成し、psqlのINSERT1本でDispatch（`smoke110-1787692055-f8a5fbc3`、DB既定の`status=draft`のまま、直接UPDATEはせず）を手挿入（06:07:35.92 JST）。`draft`は06:07:41.493〜06:09:11.699（約90秒）`lastPolledAt`がNULLのままでゲート1停止を確認。06:09:58.789に`approve-target`（`/tmp/devrelay-smoke-1_10.txt`作成→確認→削除のみ、リポジトリ禁止を明記した指示文）を実行し`submitting`（06:09:58.823）へ、`planning`（06:10:07.526、`submissionId=cmt95s7jt09fzwsy1793mazhj`）へ自動遷移。今回は実coreの`get_plan`が極めて速く確定し、`awaiting_approval`（06:10:37.722、submitting からわずか約39秒後）へ自動遷移した。`awaiting_approval`は06:10:49.303〜06:12:11.747（約82秒）自動通過ゼロを確認し、06:12:15.291に`approve-plan`実行→`approving`（06:12:15.320）→`building`（06:12:37.817、約22.5秒後）へ。`building`は初回ポーリング（`lastPolledAt`がNULLのため即時、ただしworkerの30秒ループ粒度の制約で実際は約30秒後）で早くも解決し、06:13:07.979に`done`へ遷移、`buildId=cmt95vpoo09gowsy16u7ten2s`を取得した。Dispatch挿入(06:07:35.92)から`done`(06:13:07.979)までの総実測所要時間は約5分32秒。worker停止後、`/tmp/cycle1.10-probe.mts`という使い捨ての読み取り専用プローブ（`coreClient.getBuildStatus`/`getPlan`を直接呼ぶだけでDBは一切書かない）で当該`submissionId`の生応答を事後キャプチャした：`getBuildStatus`は`{"phase":"done","buildId":"cmt95vpoo09gowsy16u7ten2s","summary":"devrelay-manager の worker 疎通確認を実施し、/tmp/devrelay-smoke-1_10.txt にファイル作成、内容一致確認、削除確認が全て成功。リポジトリへの変更はなし。","done":true}`、`getPlan`は`{"status":"ready", ...}`。（注: このプローブ呼び出しはworkerが実際に受け取った応答そのものではなく、`done`確定後に同一submissionIdへ改めて投げた読み取り専用の事後確認であることを明記する。）

## 検証
DB上のタイムスタンプにはDB既定値（`CURRENT_TIMESTAMP`、セッションtz=Asia/Tokyoで実質JSTのnaive値）とアプリ書き込み値（`new Date()`のnaive UTC値、+9h換算が必要）の食い違いが1.9同様に再確認された（`SHOW timezone`で`Asia/Tokyo`を確認、`submitting`のDB生値`2026-08-25 21:09:58.823`に+9hして`06:09:58.823 JST`と算出、実際の`approve-target`呼び出し時刻06:09:58.789と数十ms差で整合）。ゲート1・2はいずれも自動通過ゼロを実測。`classifyBuildResult`のロジック（`done===true`かつ`phase==='done'`は`phase==='succeeded'||'success'||'done'`の分岐に一致）が実coreの生語彙`phase:"done"`を正しく`succeeded`へ分類し、実際に`building→done`へ遷移したことと整合する。1.9で確認済みの`classifyPlanResult`（`status:"ready"`→`ready`）も本サイクルで再確認された。自己ループは今回発生せず、`~/.claude/plans/velvet-snacking-cerf.md`（97行、末尾も投入内容のまま）に外部からの追記が無いことを確認した——投げ先を自己以外に変えたことで1.9の主要な障害要因が解消されたことの直接的な証拠。worker停止後`ss -ltnp`で3100/3101ともLISTENなし、`git status --short`は`.devrelay/`/`.devrelay-files/`のみ（1.9時点から既存のuntracked、差分ゼロ）、`pnpm typecheck`エラー0、`pnpm test`70件全pass。`.env`は無変更、環境変数はコマンド行プレフィックスのみで恒久設定変更ゼロ。手挿入したThread（`cmt95orkh00008bq2m73w6zzz`）とDispatch（`smoke110-1787692055-f8a5fbc3`、最終状態`done`）は監査の原子として削除せず残す。ローカルの`/tmp/cycle1.10-*`（`target.log`/`dispatch-id.txt`/`thread.json`/`projects.json`/`approve-target.json`/`approve-plan.json`/`worker-1.log`/`probe.mts`）も一時物としてそのまま残す。

## 発見
①今回は**自己ループが再発せず**、`test010`は独立した実行として`submitInstruction`を受け取り、実際に`/tmp/devrelay-smoke-1_10.txt`の作成・内容確認・削除を（対象マシン側で）遂行したことがcoreの`summary`から確認できた——1.9の主要な障害要因（投げ先が自分自身）が投げ先変更だけで解消されることを実証した。②`classifyBuildResult`は実coreの応答語彙`phase:"done"`（`succeeded`/`success`ではなく`done`）に対して正しく`succeeded`へ分類できた。実装コード（`core-result.ts`）は`phase==='done'`を明示的に扱っており、コメントで自認していた「build を走らせないと全網羅を確認できない」というリスクは本サイクルでこの1語彙分については解消された（`failed`/`unknown`側の語彙は依然未検証のまま残る——今回のタスクが失敗しないトリビアルな内容だったため）。③1.9で見えたバックオフ階段（2→3→5分キャップ）は本サイクルでは観測されなかった——`building`が初回ポーリング（lastPolledAt null による即時扱い、実測は worker の30秒ループ粒度で約30秒後）で即解決したため、2回目以降のポーリングが発生しなかった。これはバグではなく、実coreのタスク完了速度に依存した設計どおりの挙動である。④DBタイムスタンプの表記不整合（DB既定値=セッションtz実質JST・アプリ書き込み値=naive UTC）は1.9からの既知の観測点として再確認されたのみで、新規の発見ではない。⑤重複devlog（`bdb71b7`）の出自: `git show --format=fuller bdb71b7`でauthor/committerとも`keisuke <keisuke@devrelay.io>`、`AuthorDate 2026-08-25 07:56:55 +0900`——本エージェント自身の`b23be8a`（07:56:31）のわずか24秒後で、かつ本ワークツリーの`git reflog`に`HEAD@{0}`として記録されていることを確認した。`2026-08-25_075556.md`の中身は同一Thread/同一Dispatch/同一submissionIdを記述しており、別実行の証跡ではなく**同一実行の第二記録**（1.9の自己ループで注入された側のセッションが書いたものである可能性が高い）と判断した。内容は矛盾ではなく相補的（075556側は2回目のkillやPID再割当など075519側が「原因不明のクラッシュ」とだけ書いた事象の内訳を持つ）。方針として`git revert`・ファイル削除は行わず、両devlogとも保全した上で`INDEX.md`の当該2行に注記のみを追加し（新規行は増やさない）、`075519`側に正・権威記録、`075556`側に同一実行の第二記録・自己ループ由来の疑いである旨を明記した。
