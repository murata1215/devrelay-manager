# 2026-08-23 19:18 JST ｜ サイクル1.1: council-run.sh 骨格実装

要求: doc/council-orchestration-spec.md に基づき、codex 実行なしで完結する範囲（決定的ループ本体・verdict パーサ・COUNCIL_META・コスト合算・安全弁）で council-run.sh を実装する。実 codex への ask は週間上限（2026-08-24 22:17 JST まで）のため行わない。

実行: scripts/lib/council-lib.sh（純粋関数8個: deliberationId採番、COUNCIL_METAヘッダ生成、COUNCIL_VERDICTパーサ、収束判定、usageData合算、ask.sh前置き除去、残論点抽出、AI検出）と scripts/council-run.sh（決定的ループ本体、--council オプトイン、静かなフォールバック、最大反復ハードキャップ10）を実装。tests/mock-ask.sh（ask.sh互換モック）と tests/fixtures/（verdict8種+proposal2種+usageサンプル）を作成し、tests/run-tests.sh（外部依存なし自作ランナー）で検証。事前調査で ask.sh の --ai が実装済み（質問専用）と判明したため spec §3 の陳腐化注記を1行更新。

検証: tests/run-tests.sh で55アサーションすべてパス（verdictパーサ8fixture×3項目=24、収束判定8件、ループ終了条件4パターン(1ラウンド収束/2ラウンド収束/max_rounds打ち切り/ハードキャップ超過エラー)、コスト合算2パターン(合算3020トークン一致/cost_limit途中停止)、--council省略時の後方互換、AI2種未満の静かなフォールバック、人間提示が最終1回のみ、ログ4ファイル生成+命名規則）。実装過程で2件のバグを自己発見・修正: (1) 同一秒内の複数ラウンドでログファイル名が衝突する問題→ファイル名にround番号を追加、(2) max_roundsで打ち切られた際に実行ラウンド数の表示が実数より1多くなる問題→EXECUTED_ROUNDS変数で修正。

発見: ask.sh は usageData を返さないため、コスト合算はロジックのみ実装し --usage-file による fixture 検証に留めた（ライブ結線は codex 週間上限解除後の次サイクル）。「AI 2種未満」判定はリモートマシンの利用可能AI一覧を問い合わせるAPIが存在しないため、COUNCIL_AIS環境変数優先 + ローカルCLIプローブ（command -v claude/codex）で代替。残論点抽出（spec §6）は追加LLMコール禁止の制約から `- [blocker]`/`- [major]` 記法規約に依拠する設計とした（spec未定義箇所のv1実装判断）。
