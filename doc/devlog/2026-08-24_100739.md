# 2026-08-24 10:07 JST ｜ サイクル1.3: manager サービス骨格立ち上げ

要求: devrelay-manager を「起動して喋る空のサービス」まで立ち上げる。pnpm モノレポ + Fastify + Prisma/PostgreSQL で Thread/Message/Dispatch テーブルと最小 API を用意し、orchestrator・core接続・フロント・認証は入れない。既存の scripts/ tests/ rules/ は無変更。

実行: 事前調査で pnpm 10.34.3 が既に利用可能、PostgreSQL 16.15 稼働中・CREATEDB権限ありを確認。root package.json / pnpm-workspace.yaml / tsconfig.json を作成し、apps/server に Fastify(4.29.1) + Prisma(5.22.0) + @fastify/cors(9.0.1、Fastify4系対応に固定) + typescript(5.9.3固定) を導入。Prisma スキーマに Thread/Message/Dispatch(id=cuid、camelCase、@@index付き) を定義し `prisma migrate dev --name init` を実行、3テーブル+8インデックスが生成された。root の .env(DB_HOST等5キー+既存DB_URL) を apps/server/.env へ移動し、5キーから組み立てた DATABASE_URL を1行追記（既存キーは無改変）。Fastify に /health, /threads(CRUD), /threads/:id/messages を実装。

検証: `pnpm build` (tsc) がエラーなしで通過。dev サーバー起動後、/health→{"status":"ok"}、Thread作成→一覧→取得、Message追記→一覧、存在しないIDで404、を全て実測で確認。psqlで3テーブル+全indexの実在を確認。テストデータはcleanup済み（3テーブルとも0件に復帰）。`bash tests/run-tests.sh` で既存55件が引き続き全pass、bash資産への非破壊を確認。git diffでapps/配下と設定ファイルのみが追加されたことを確認し、.envはcommit対象外(.gitignore)であることも確認済み。

発見: root .env には当初把握していた5キーに加えて既存の DB_URL キーが1本あることが移動時に判明した(接続文字列と思われるが未使用のまま温存、値は無改変)。また @fastify/cors は無指定だとlatest(11.3.0)がFastify5系専用のfastify-plugin@^6に依存し、指定のFastify4系と組み合わせると起動時にthrowすることをレジストリ調査で事前検知し、9.0.1に固定して回避した。typescriptも無指定だと7.0.2が入るため5.9.3に固定。tsxはesbuildのpostinstallがブロックされた状態でも問題なく動作した。
